<UserControl x:Class="DoAnNhom.CardBatDongSan"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:DoAnNhom"
             mc:Ignorable="d"
             Width="320" Height="380">

    <UserControl.Resources>
        <!-- Gold Brush -->
        <LinearGradientBrush x:Key="GoldBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F5D77A" Offset="0"/>
            <GradientStop Color="#C9A227" Offset="1"/>
        </LinearGradientBrush>

        <!-- Button Style -->
        <Style x:Key="LuxBtn" TargetType="Button">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#3A3A3A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="12,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#C9A227"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#202020" CornerRadius="12" Padding="0" Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="200"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Ảnh BĐS -->
            <Border Grid.Row="0" CornerRadius="12,12,0,0" ClipToBounds="True">
                <Image x:Name="imgHinh" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
                <Border.Background>
                    <SolidColorBrush Color="#202020"/>
                </Border.Background>
            </Border>

            <!-- Nội dung -->
            <Grid Grid.Row="1" Margin="18,15,18,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Tiêu đề -->
                <TextBlock x:Name="txtTieuDe"
                           Grid.Row="0"
                           FontWeight="Bold" FontSize="16"
                           Foreground="#FFFFFF"
                           TextWrapping="Wrap"
                           Margin="0,0,0,10"
                           MaxHeight="48"/>

                <!-- Giá & Diện tích -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock x:Name="txtGia"
                               FontWeight="Bold"
                               Foreground="#F5D77A"
                               FontSize="18"
                               Margin="0,0,15,0"/>
                    <TextBlock x:Name="txtDienTich"
                               Foreground="#AAAAAA"
                               FontSize="14"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Địa chỉ -->
                <TextBlock x:Name="txtDiaChi"
                           Grid.Row="2"
                           Foreground="#888888"
                           TextWrapping="Wrap"
                           FontSize="13.5"
                           Margin="0,0,0,15"/>

                <!-- Nút chức năng -->
                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnYeuThich"
                            Content="❤️"
                            Width="42" Height="36"
                            Margin="0,0,10,0"
                            Style="{StaticResource LuxBtn}"
                            Click="BtnYeuThich_Click"/>

                    <Button x:Name="btnChiTiet"
                            Content="Chi tiết"
                            Width="90" Height="36"
                            Style="{StaticResource LuxBtn}"
                            Click="BtnChiTiet_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>